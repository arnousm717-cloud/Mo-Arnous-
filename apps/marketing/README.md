# apps/marketing (placeholder)

Intentionally not scaffolded as a full app yet. There is no marketing site to build until well after the core product ships (it doesn't appear until the later phases of `docs/09-Development-Roadmap.md`), so standing up a second Next.js app now would be unused boilerplate.

When there's real content to put here, scaffold it the same way `apps/web` was built — a separate Next.js app, its own `package.json`, sharing `packages/config` and `packages/ui`.
